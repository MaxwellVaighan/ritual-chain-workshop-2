# Payout Notes

The payout formula became easier for me once I tested it with small numbers.

The basic idea is proportional:

stake × totalPool ÷ winningPool

I also checked what happens when the winning pool is empty.
